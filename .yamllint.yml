# Relax rules that standard GitHub Actions YAML trips.
extends: default
rules:
  line-length: disable
  document-start: disable
  truthy:
    check-keys: false # allow the `on:` workflow key
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
