{
  "name": "jync-plugin",
  "version": "0.1.0",
  "description": "Obsidian plugin: sync a vault subtree to a JMAP FileNode backend.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "obsidian": "^1.13.1",
    "typescript": "^7.0.2"
  }
}
